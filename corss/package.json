{
  "name": "base-aptos-cctp-bridge",
  "version": "1.0.0",
  "description": "Base到Aptos的CCTP跨链桥实现",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "example": "ts-node examples/complete-example.ts",
    "test": "jest",
    "test:all": "./test-all.sh",
    "test:cross-chain": "npm run example cross-chain",
    "test:balance": "npm run example balance",
    "test:account": "npm run example create-account",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist",
    "move:build": "aptos move compile",
    "move:test": "aptos move test",
    "move:publish": "aptos move publish"
  },
  "keywords": [
    "cctp",
    "cross-chain",
    "base",
    "aptos",
    "usdc",
    "circle",
    "bridge"
  ],
  "author": "cross_chain_dev",
  "license": "MIT",
  "dependencies": {
    "@aptos-labs/ts-sdk": "^1.0.0",
    "ethers": "^6.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}